# Need to get a job

The Steward's spec, written on the floor (Get a job).

Scan the local job boards and apply for 20 jobs. I want to run it once per day, scrape linkedin and Indeed and then return an email of the jobs. Then open each job in a browser tab and prepare a custom resume.
