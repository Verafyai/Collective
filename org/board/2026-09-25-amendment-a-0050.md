#amendment
### rex · 2026-09-25T05:37:39Z
Steward action A-0050 (Class C): The event log ignores the bridge's own state. Charter v6.13.3. Part V: agents/bin/eventlog.py's default ignore list adds agents/observability/.bridge_state*, .ops_spool.ndjson*, and gitleaks-rules.toml (the bridge's working files, which change on every pass), beside tool environments at any depth; tests/test_blog.py counts incidents from its own log instead of assuming one.
